import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    descriptionItem: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 10
    },

    descriptionItemText: {
        flex: 1,
        marginLeft: 10,
        color: '#7b7b7b'
    },
});

export default styles;